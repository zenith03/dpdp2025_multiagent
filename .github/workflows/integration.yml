name: Integration Test

on:
  release:
    types: [published]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    # cognizant-ai-lab/build-common 1.0.9
    uses: cognizant-ai-lab/build-common/.github/workflows/_python-quality-gate.yml@b14b3de23cb9aca02b2165e57542335e0c8f5304
    with:
      python-version: '3.12'
      # No lint steps in the original workflow
      lint-command-override: 'true'
      run-pytest: false
      test-command-override: >-
        export OPENAI_API_VERSION="${{ vars.OPENAI_API_VERSION }}" &&
        export AGENT_TOOL_PATH="coded_tools/" &&
        export AGENT_MANIFEST_FILE="registries/manifest.hocon" &&
        export AGENT_TOOLBOX_INFO_FILE="neuro_san_studio/toolbox/toolbox_info.hocon" &&
        export PYTHONPATH="${PYTHONPATH:+$PYTHONPATH:}." &&
        pytest --capture=no -m "integration"
    secrets:
      OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
      SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
