# Copyright © 2025-2026 Cognizant Technology Solutions Corp, www.cognizant.com.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#
# END COPYRIGHT

# The schema specifications for this file are documented here:
# https://github.com/cognizant-ai-lab/neuro-san/blob/main/docs/agent_hocon_reference.md

{
    # Optional metadata describing this agent network
    "metadata": {
        "description": "A coding problem solver that uses checklist middleware to plan and track its own reasoning steps. The checklist is the agent's internal scratchpad — not visible to the user as a task list.",
        "tags": ["basic", "checklist", "example", "middleware"],
        "sample_queries": [
            "Given a list of integers, find all pairs that sum to a target value.",
            "Write a function to detect a cycle in a linked list.",
            "Implement an LRU cache with O(1) get and put operations.",
            "Given a binary tree, return the level-order traversal as a list of lists."
        ]
    },

    include "registries/expertise_scoping_instructions.hocon",

    # Load the shared LLM configuration from a single source of truth.
    # This allows users to change the model in one file rather than
    # modifying the configuration for each agent network.
    # Note that the file path here is relative to the root level of the repo.
    include "config/llm_config.hocon",

    "tools": [
        # This first agent definition is regarded as the "Front Man", which
        # does all the talking to the outside world/client.
        #
        # Some disqualifications from being a front man:
        #   1) Cannot use a CodedTool "class" definition
        #   2) Cannot use a Tool "toolbox" definition
        #
        # Besides the first agent being the front man, these tool definitions
        # do not have to be in any particular order. How they are linked and
        # call each other is defined within their own specs.
        {
            "name": "coding_problem_solver",

            "function": {
                # The description acts as an initial prompt.
                "description": """
I solve coding problems step by step.
Give me any algorithm or data structure problem and I will plan my approach,
work through each step methodically, and deliver a clean solution with explanation.
"""
            },

            # Here the solver agent is responsible for both planning and execution — it creates
            # the checklist itself before writing any code. An alternative design would split
            # these concerns: a dedicated planner agent decomposes the problem and comes up with a plan,
            # then hands off to a separate executor agent to work through it.
            "instructions": ${expertise_scoping_instructions} """
You are an expert software engineer who solves coding problems by following a disciplined,
step-by-step process. You use a private internal checklist to plan and track your own
reasoning — it is YOUR work plan, not a list of instructions for the user.

## Your Internal Workflow

**Step 1 — Plan first, always.**
Before writing any code, create checklist to decompose the problem into your own
reasoning steps. Typical steps you might include (adapt to the problem):
  - Understand the problem and constraints
  - Identify input/output types and edge cases
  - Choose data structure(s)
  - Design the algorithm (describe approach, e.g. two-pointer, BFS, DP)
  - Analyse time and space complexity
  - Write the solution
  - Run solution with example inputs via code_interpreter
  - Fix any errors found and re-run
  - Refine or optimise if needed

**Step 2 — Work through your checklist.**
For each item on your checklist:
  1. Update checklist item with status "in_progress" before you begin that step.
  2. Do the work: think through it, write the relevant output.
  3. Update checklist item with status "done" (and a brief `notes` value capturing
     the key decision or result of that step) before moving to the next.

**Step 3 — Validate with code_interpreter.**
Before presenting the solution, ALWAYS call `code_interpreter` to execute the code with:
  - The provided example inputs
  - At least one edge case (empty input, single element, duplicates, etc.)
If the output is wrong or an exception is raised, update the affected checklist step back
to "in_progress", fix the code, and re-run until all tests pass.

**Step 4 — Deliver the result.**
Once all checklist items are done and the code has been verified, present the final solution:
  - Clean, working code with a clear language/framework choice
  - A short explanation of the approach and why it was chosen
  - Time and space complexity
  - The actual output from code_interpreter as the worked example

## Rules
- ALWAYS create checklist before writing any code. Never skip planning.
- ALWAYS call `code_interpreter` to run the code before delivering the answer. Never present unverified code.
- Mark each step "in_progress" then "done" as you go — never bulk-update at the end.
- Use `notes` to record the key insight or decision from each step (1–2 sentences max).
- If your approach changes mid-solution, edit checklist item to rewrite the affected
  step(s) in place — do NOT recreate the whole checklist and lose completed progress.
- Never show the checklist to the user. It is your internal plan only.
- If the problem is ambiguous, ask ONE focused clarifying question before planning.
- Prefer well-known idiomatic solutions; note trade-offs when alternatives exist.
""",

            "middleware": [
                {
                    # AgentChecklistMiddleware stores a checklist in the agent's memory
                    # (instance variable) and injects the current state into the system
                    # prompt before every model call.
                    #
                    # The checklist is the agent's private work plan — not a user-facing list.
                    # It keeps the agent on track across multiple reasoning steps without
                    # relying solely on context window recall.
                    #
                    # Registered tools available to the agent:
                    #   - create_checklist(items)                        — create or replace the plan
                    #   - update_checklist_item(index, status, notes?)   — advance a step
                    #   - edit_checklist_item(index, new_item)           — rewrite a step when the plan changes
                    #
                    # The current checklist state is always injected into the system prompt
                    # automatically by awrap_model_call — no read-back tool needed.
                    "class": "middleware.agent_checklist_middleware.AgentChecklistMiddleware",

                    "args": {
                        "checklist_title": "My Solution Plan",
                        # When false (default), checklist tool responses are intercepted and
                        # not written to the chat context, keeping the conversation history clean.
                        # The agent still sees the updated checklist via system prompt injection.
                        "keep_checklist_in_context": true,
                        # Declaring "progress_reporter" here causes the framework to inject
                        # a ProgressJournal instance, enabling real-time progress updates
                        # (0.0–1.0) streamed to the client as the checklist advances.
                        "progress_reporter": true
                    }
                }
            ],
            "tools": ["code_interpreter"]
        },

        {
            "name": "code_interpreter",            
            "toolbox": "openai_code_interpreter",

            # --- Optional Arguments ---
            "args": {
                # OpenAI code interpreter uses this model. Default to gpt-4o-2024-08-06.
                "openai_model": "gpt-4.1"
            }
        },
    ]
}
